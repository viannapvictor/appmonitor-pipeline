name: Deploy to Production

on:
  push:
    branches: [ci/setup]

jobs:
  deploy:
    name: Deploy job (needs approval)
    runs-on: ubuntu-latest
    environment: production

    steps:
      - uses: actions/checkout@v4

      - name: Simular deploy
        run: echo "Deploying to $PROD_DOMAIN"